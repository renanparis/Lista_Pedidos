<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.paris.hayorders">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_bons_ventos"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ListOrdersActivity"></activity>
        <activity android:name=".activity.CustomerForm" />
        <activity android:name=".activity.CustomerListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>